# 任务拆分 — 需求名称

> 每个 task = 可独立提交的原子变更（3-5 个文件）
> 精确到文件路径和函数签名

## 前置条件
- [ ] spec 已确认（§9 已清空）
- [ ] 已创建 feature 分支：`git checkout -b feature/<变更名>`
- [ ] 本地环境就绪

---

## Task 1: 任务名
- **目标**：一句话描述
- **覆盖功能点**：F01/F02（无则写"无"）
- **涉及文件**：
  - `path/to/File.java` — 新增/修改，做什么
- **关键签名**：
  ```java
  public ResultDTO doSomething(Long id) { }
  ```
- **验证命令**：
  ```bash
  curl -s http://localhost:8080/api/xxx | python3 -m json.tool
  ```
- **Git commit**：`[变更名] T1: <中文简述>`
- 状态：待完成

### 🔒 完成时必填字段（任一留空 = gate review 不通过）

> 自评不是备注，是 gate 输入。状态标为 ✅ 的 task：自评分低于 100、未完成项非"无"、已知缺陷非"无"、简化/降级非"无"时，必须停下修复；除非用户明确接受降级并记录到 log.md。

**1. 实际验证结果**（粘贴原始输出，必须以 `$ ` / `>>> ` / `HTTP/` 开头）
```
$ curl -i http://localhost:8080/api/xxx
HTTP/1.1 200 OK
{"code":200,"data":{...}}
```

**2. 实际文件清单**
- 计划：（从上方"涉及文件"复制）
- 实际：（git diff --name-only 输出）
- 遗漏：（无 / 列出原因）

**3. 未实现声明**（必填，无则写"无"）
- 本 task 未完成的功能点：
- 已知缺陷或 TODO：
- 简化或降级处理：
- 用户确认接受降级：否（若是，必须写"是，见 Dxxx"，并在 log.md 用户决策记录中存在对应 Dxxx）

**4. 自评**
- 给用户拿这个代码，他能干什么/不能干什么：
- 自评分（0-100）：__/100（扣分理由必填）

---

## Task 2: 任务名
（同上格式）

---

## 偏差记录
> 执行中任何 scope 偏差立即记录，不可留空后补

| Task | 偏差内容 | 原因 | 用户确认 |
|------|---------|------|---------|
| （无偏差则写"无"） | | | |

## 变更摘要
> apply 完成后填写

- **实测**：N 个文件新增，M 个修改
- **未实现功能点**：（必填，无则写"无"）
- **遗留 TODO**：
