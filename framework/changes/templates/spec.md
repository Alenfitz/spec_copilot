# 需求名称

> status: propose | apply | review | done
> created: YYYY-MM-DD
> complexity: 🟢 轻 | 🔴 重

## 1. 背景与目标
为什么做 + 做完后用户能干什么（可验证的结果描述）

## 2. 代码现状
> 每个结论必须有代码出处（文件路径 + 类名/方法名）

### 2.1 相关入口与链路
### 2.2 现有实现
### 2.3 风险点

## 3. 功能点
> 每个功能点必须有唯一 ID（F01/F02...）、一句话验收标准

- **F01** — 描述（输入→处理→输出）
  - 验收：用户从 X 页面操作 Y，看到 Z 结果

## 4. 业务规则
> 精确描述（含边界条件、错误文案）

- **V01** — 例：签发时间必须 ≤ 计划开工时间，否则返回"签发时间不得晚于计划开工时间"

## 5. 数据变更
| 操作 | 表名 | 字段/索引 | 说明 |

## 6. 接口契约

### 6.1 接口覆盖矩阵
| API ID | Method | Path | 前端调用方 | 后端实现入口 | 关联功能点 |
|-------|--------|------|-----------|-------------|----------|
| API01 | GET | /api/xxx | `src/api/xxx.ts#getXxx` | `XxxController#getXxx` | F01 |

### 6.2 接口详情（每个新增接口必填）

```
GET /api/xxx
入参：
  - paramA: String  // 说明，枚举值、默认值
出参：
  {
    "code": 200,
    "data": { "fieldA": "...", "fieldB": 0 }
  }
```

## 7. 影响范围
受影响的页面/模块/外部依赖

## 8. 测试策略
- **正向用例**：（至少 1 条）
- **异常用例**：（至少 1 条）
- **幂等验证**：（有副作用接口必填）

## 9. 待澄清
- [ ] 问题 1：（全部解决后才能进入 /spec:apply）

## 10. 技术决策

## 11. 执行日志
| Task | 状态 | 实际改动文件 | 备注 |

## 12. 审查结论
> v4.0.18 起推荐写入独立 review.md；apply 后不得改低本 spec 的需求契约区。

- Spec 合规：✅/❌（功能点真实覆盖率 X/Y）
- Code Quality：Critical: 0 / Important: N
- 结论：通过 / 需修复

### 12.1 功能点闭环
| 功能点 | 前端证据 | 后端证据 | 结论 |
|-------|---------|---------|------|
| F01 | `src/views/...` | `XxxController#...` | done |

## 13. 确认记录
- 时间：
- 确认人：

---

## 附录：Spec 质量自检

- ❌ 功能点写成需求标题，缺输入→处理→输出
- ❌ 业务规则用模糊词："尽快"、"适当"、"一般"
- ❌ 接口入参没写类型/必填/枚举
- ❌ §9 是空的但 spec 里有大量"应该"、"可能"
- ❌ 代码现状写"应该是"、"一般用" — 没有 grep 证据
